import { FindOptions } from 'sequelize';

export const myRoles = (userId: string): FindOptions => ({
});

export const roleById = (id: string) => ({
});

export const ByTitle = (title: string): FindOptions => ({
});

export const roles = {
};
